lupa-agent/
├── agent.py                  # entry point, CLI loop, Strands wiring
├── core/
│   ├── model.py              # OllamaModel wrapper, always /no_think
│   ├── session.py            # FileSessionManager setup
│   └── mcp.py                # MCP filesystem client → ./data/
├── tools/
│   ├── __init__.py           # registers all @tool functions
│   ├── lookup.py             # lookup_municipality(name)
│   ├── filter.py             # filter_by_risk(category)
│   ├── summarize.py          # summarize_findings()
│   ├── top_vulnerable.py     # top_vulnerable(n)
│   └── port_query.py         # port_scan_query(port_or_service)
├── data/
│   └── municipalities.json   # pre-processed, risk_score baked in
├── sessions/
├── prompts/
│   └── system.md
└── requirements.txt